<#assign licenseFirst = "/*">
<#assign licensePrefix = " * ">
<#assign licenseLast = " */">
<#include "../Licenses/license-${project.license}.txt">

<#if package?? && package != "">
package ${package};

</#if>
import org.debux.webmotion.server.WebMotionFilter;

/**
 *
 * @author ${user}
 */
public class ${name} extends WebMotionFilter {

    public void filter() {
        // Before filter
        doProcess();
        // After filter
    }

}
